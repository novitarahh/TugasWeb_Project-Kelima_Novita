/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DropdownNovitaDirective } from './dropdownNovita.directive';

describe('Directive: DropdownNovita', () => {
  it('should create an instance', () => {
    const directive = new DropdownNovitaDirective();
    expect(directive).toBeTruthy();
  });
});
